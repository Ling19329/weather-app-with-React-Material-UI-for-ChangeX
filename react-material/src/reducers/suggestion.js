
const INITIAL_STATE = [
  { label: 'Belgrade' },
  { label: 'Novi Sad' },
];
function suggestionReducer(state = INITIAL_STATE, action) {
    switch(action.type) {
      default : return state;
    }
  }
  
  export default suggestionReducer;